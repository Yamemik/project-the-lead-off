import "./SeeAllButton.scss";

const SeeAllButton = () => {
    return (
        <div className="seeAllButton">
            <button className="seeAllButton__btn">Посмотреть всё</button>
        </div>
    );
};

export default SeeAllButton;
